"use client";

import { CalendarClock, CheckCircle2, FileText, MoreHorizontal, Plus, Search, Upload, Users } from "lucide-react";
import { useState } from "react";
import { useDemo } from "@/shared/demo/demo-provider";
import type { TaskRecord } from "@/shared/demo/types";
import { Button, Field, Modal, PageHeader, ProgressBar, StatusBadge } from "@/shared/ui/ui-kit";
import { useToast } from "@/shared/ui/toast";

export function TasksAdminPage(){const{state,dispatch}=useDemo();const{toast}=useToast();const[creating,setCreating]=useState(false);const[title,setTitle]=useState("");const[mode,setMode]=useState<TaskRecord["mode"]>("manual");function create(){if(!title.trim())return;dispatch({type:"ADD_TASK",task:{id:`task_${Date.now()}`,eventId:state.events[0]?.id??"",title,description:"Complete this item in your speaker portal.",mode,target:"contact",dueAt:"2026-09-05T06:59:00.000Z",assigned:10,completed:0,required:true}});setCreating(false);setTitle("");toast("Task published to accepted speakers")}return <><PageHeader eyebrow="PEOPLE" title="Onboarding tasks" description="Collect speaker information, files, and confirmations without spreadsheets." actions={<Button onClick={()=>setCreating(true)}><Plus size={16}/> Create task</Button>}/><div className="task-admin-grid"><section className="task-list-panel panel"><div className="data-toolbar"><label className="table-search"><Search size={16}/><input placeholder="Search tasks"/></label><span className="row-count">{state.tasks.length} active</span></div>{state.tasks.map((task)=>{const progress=Math.round(task.completed/task.assigned*100);return <article className="admin-task-row" key={task.id}><span className={`task-mode-icon ${task.mode}`}>{task.mode==="file_request"?<Upload size={18}/>:task.mode==="form"?<FileText size={18}/>:<CheckCircle2 size={18}/>}</span><div className="admin-task-main"><div><h3>{task.title}</h3><StatusBadge value={task.mode.replace("_"," ")}/></div><p>{task.description}</p><div><span><CalendarClock size={13}/> Due {new Date(task.dueAt).toLocaleDateString("en-US",{month:"short",day:"numeric"})}</span><span><Users size={13}/> {task.assigned} assigned</span></div></div><div className="admin-task-progress"><div><b>{task.completed}/{task.assigned}</b><span>{progress}%</span></div><ProgressBar value={progress} tone={progress>75?"green":"purple"}/></div><button className="icon-button"><MoreHorizontal size={18}/></button></article>})}</section><aside className="task-insights panel"><header className="panel-header"><div><h2>Completion overview</h2><p>Across accepted speakers</p></div></header><div className="completion-ring"><svg viewBox="0 0 100 100"><circle cx="50" cy="50" r="40"/><circle className="value" cx="50" cy="50" r="40" pathLength="100" strokeDasharray="68 100"/></svg><div><strong>68%</strong><span>complete</span></div></div><div className="task-legend"><div><i className="green"/><span>Complete</span><b>24</b></div><div><i className="purple"/><span>In progress</span><b>8</b></div><div><i className="amber"/><span>Overdue</span><b>4</b></div></div><Button variant="secondary" onClick={()=>toast("Reminder queued for overdue assignments")}>Send overdue reminders</Button></aside></div><Modal open={creating} onClose={()=>setCreating(false)} title="Create an onboarding task" description="Assign once to contacts or per accepted submission." footer={<><Button variant="secondary" onClick={()=>setCreating(false)}>Cancel</Button><Button disabled={!title.trim()} onClick={create}>Create task</Button></>}><div className="form-stack"><Field label="Task title" required><input autoFocus value={title} onChange={(e)=>setTitle(e.target.value)} placeholder="e.g. Upload final slides"/></Field><Field label="Completion mode"><div className="choice-cards compact">{(["manual","form","file_request"] as const).map((value)=><button key={value} className={mode===value?"active":""} onClick={()=>setMode(value)}><b>{value.replace("_"," ")}</b><small>{value==="manual"?"One-click confirmation":value==="form"?"Structured questions":"Document upload"}</small></button>)}</div></Field><div className="form-grid"><Field label="Target"><select><option>Accepted speakers</option><option>Accepted submissions</option></select></Field><Field label="Due date"><input type="date" defaultValue="2026-09-05"/></Field></div></div></Modal></>}
