import{initialDemoState}from"@/shared/demo/seed";import{data}from"../../../_lib";export async function GET(){const speakers=initialDemoState.speakers.filter((item)=>item.confirmation==="confirmed").map((item)=>({id:item.id,eventId:item.eventId,firstName:item.firstName,lastName:item.lastName,company:item.company,title:item.title,bio:item.bio,location:item.location,website:item.website,linkedin:item.linkedin,avatar:item.avatar,tags:item.tags}));return data(speakers,{count:speakers.length})}
