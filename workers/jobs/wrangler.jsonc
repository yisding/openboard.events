{
  "$schema": "../../node_modules/wrangler/config-schema.json",
  "name": "sb-jobs-local",
  "main": "index.ts",
  "compatibility_date": "2026-08-01",
  // Kept for the one-release public rollback adapter while RPC is proved.
  "compatibility_flags": ["nodejs_compat", "global_fetch_strictly_public"],
  "keep_vars": true,
  "triggers": { "crons": ["* * * * *"] },
  "services": [
    { "binding": "WEB_JOBS", "service": "sb-web-local", "entrypoint": "JobsEntrypoint" }
  ],
  // Local/package and deploy scripts provide APP_BASE_URL; never commit a guessed workers.dev hostname.
  "observability": { "enabled": true },
  "env": {
    "preview": {
      "name": "sb-jobs-preview",
      "triggers": { "crons": ["* * * * *"] },
      "services": [
        { "binding": "WEB_JOBS", "service": "sb-web-preview", "entrypoint": "JobsEntrypoint" }
      ],
      "observability": { "enabled": true }
    },
    "production": {
      "name": "sb-jobs",
      "triggers": { "crons": ["* * * * *"] },
      "services": [
        { "binding": "WEB_JOBS", "service": "sb-web", "entrypoint": "JobsEntrypoint" }
      ],
      "observability": { "enabled": true }
    }
  }
}
